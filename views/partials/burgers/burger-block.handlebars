<li>
	{{burger_name}}
	{{#if devour}}
		<button class="devour-btn" data-id="{{id}}">
			Devour it!
		</button>
		{{else}}
		<button class="delete-btn" data-id="{{id}}">
			Delete
		</button>
	{{/if}}
</li>
