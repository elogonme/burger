<h1>Eat-Da-Burger!</h1>

<h2>Burgers list</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devour=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devour=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger!</h2>
<form id="create-form">

  <div class="form-group">
    <textarea type="text" name="burger" id="burger"></textarea>
  </div>

  <button type="submit">Add Burger</button>
  <p>Enter the burger you want to eat and click Add button</p>
</form>